// import type { useNavigate } from "react-router";

export const UpdatePayments = () => {
    // const navigate = useNavigate();
    return (
        // <h1>${navigate.}</h1>
        <h1>payments</h1>
    );
};
